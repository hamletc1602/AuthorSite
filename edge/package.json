{
  "name": "edge",
  "version": "1.0.0",
  "description": "",
  "main": "edge.js",
  "scripts": {
    "install-for-aws": "npm i --platform=linux --arch=x64",
    "clean-install-for-aws": "npm ci --platform=linux --arch=x64",
    "pack": "rm -f edge.zip & npm-pack-zip",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx eslint *.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "aws-sdk": "^2.1231.0",
    "eslint": "^8.26.0",
    "npm-pack-zip": "^1.3.0"
  },
  "dependencies": {
    "fast-deep-equal": "^3.1.3",
    "mime": "^3.0.0"
  },
  "bundleDependencies": [
    "fast-deep-equal",
    "mime"
  ]
}
